name: check-esdt
version: 1.0.51
summary: Check if branch name and commit message contains a specific format (ESDT-[0-9]+
tags:
  - branch name
  - commit message
  - specific format
  - pull request check
properties:
  - name: REPO_USER
    type: string
    required: true
  - name: REPO_NAME
    type: string
    required: true
  - name: TOKEN
    type: string
    required: true
